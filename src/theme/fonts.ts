export type FontList = Readonly<{
  regular: string;
  medium: string;
  bold: string;
}>;

export const fonts: FontList = {
  regular: "DMSans-Regular",
  medium: "DMSans-Medium",
  bold: "DMSans-Bold",
};
